version: '3'
services:
  token_management:
    build: ./map_management
    ports:
        - "7804:8000"
    restart: on-failure
    volumes:
        - .:/src
        - /etc/haproxy/maps:/etc/haproxy/maps